# Handoff 2026-09-11: Rail hardening loop, fixes landed

Branch `fix/hardening-loop-2026-09-11` in the detached worktree `~/projects/rail-harden`
(the working clone `~/projects/rail` was never touched). PR: https://github.com/zemo-g/rail/pull/72,
CI green (grep, semantic, test 22 min), awaiting Reilly's merge. Master is protected and the PR
swaps the committed seed, so the merge is his call.

## What landed

- `4572636` Seven miscompiles from the hardening loop, fixed with their checks. Every one of them
  passed 198/198, the fixed point and the CI fuzz seed before. Details: CHANGELOG.md (Unreleased),
  the loop's own record in `~/projects/rail/findings/REPORT.md` (outside the repo, "Fixes landed"),
  and the page https://claude.ai/code/artifact/81377bf2-b334-412f-ab7f-eed33dbb8c4d.
  - tail self-call arguments (bottom-test refusal, untagging fallback, raw literal): t206
  - string ordering in `_rail_lt/gt/le/ge`: t207
  - string params read as ints (call-site kinds veto raw registers, int marks, early return): t208
  - frames under-sized for sibling slots (cumulative predictor + re-size from reached slot): t209
  - floats out of containers: the float representation boundary, docs/NUMERICS.md: t210;
    closes known/mixed_int_from_fn, semantic.py fuzzes mixed arithmetic by default
  - `self` resets the arena between compile and in-process link (the 6 GB cliff)
  - compile_func keeps program-wide markers behind the locals: codegen 7.6 s to 1.7 s,
    self-compile 21.5 s to 18.2 s
- Seed `rail_native` rebuilt to a three-cycle fixed point, sha256 37b950b8...; suite 203/203.

Uncommitted: nothing (`git status --short` empty before this handoff).

## Gate (`RAIL_ARENA_MB=4096 ./rail_native test`, run at 16:48Z)

```
1.25
2.5 (exit 0)
203/203 tests passed
exit 0
```

Also green on the final binary this afternoon: three-cycle bootstrap fixed point,
`tools/fuzz/known_cases.py`, `test_semantic.py` (idle; its fake compiler has 1 s timeouts and
fails under load), `semantic.py` seeds 42 / 4242 / 77 mixed, the 190-cell type matrix (190/190),
both loop generators, generated-code benches within noise.

## Open

- Merge of PR #72: Reilly.
- `tools/x86_rt.s` keeps the string-ordering gap and knows nothing of the float boundary (noted in
  the file). Nobody; the x86 backend is not built here.
- `_rail_fval` misreads a positive subnormal double whose bits fall in [4096, 2^47) as a pointer
  (documented beside the routine). Needs a representation decision, same class as
  `known/char_from_int_nul`, which is still live.
- The call-site analysis costs +4.5 s on the compiler's own source (11.3 s to 15.9 s); paid back
  by the codegen fix but worth a look if the self-compile grows again.
- After the merge: the hourly drift audit and the daily repro witness run the WORKING clone and
  `rail-https`; both need a pull to pick up the new seed.

## Next command

```
cd ~/projects/rail-harden && gh pr view 72 --json state,mergeable   # merged? then:
cd ~/projects/rail && git pull --ff-only origin master && RAIL_ARENA_MB=6000 ./rail_native self && cmp rail_native /tmp/rail_self
```
