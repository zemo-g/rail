# Handoff 2026-09-23: types, then the collector under stress; focus shifts away

Reilly parked this thread on 2026-09-23 ("I want to shift focus for now"). Master is at
`eedb1fb`. Nothing is uncommitted and no branch is open. The open work is issue
[#116](https://github.com/zemo-g/rail/issues/116), the bulletin (APB) for what is left.

## What landed since the 2026-09-11 handoff (46 commits, all through PRs, CI green)

- **The collector under stress (#109 to #115).** It had been close to untested: at the default
  1 GB arena almost no test program fills the heap, so collector bugs passed behind correct
  answers. `RAIL_GC_STRESS=N` plus `tools/fuzz/gc_stress.py` (#110) collects every Nth
  allocation, poisons freed blocks, and compares every suite test and self-contained tree
  program with its normal run. With #109's bug put back it flags 44 suite tests. It found, and
  these PRs fixed, each with a test shown to fail without the fix:
  - #109: frames read with the caller's slot map.
  - #111: a guarded variable arm wrote past its frame.
  - #112: `split`, `str_split` and `chars` let a temporary string be freed mid-walk.
  - #113: spills outside the arena; the arena now grows in place in a 64 GB reservation.
  - #114: threads raced on the allocator and the collector.
  - #115: CI job `gc-stress`.
  The sweep is now 0 DIFF. Locally: 318 ok, 22 skipped, 5 slow (ECDSA). CI's first run: 315 ok,
  25 skipped, 5 slow, 8.5 min.
- **Types pay off (#103 to #108).** Wasm is linear in program size; imports are deduplicated;
  variable and boolean patterns work, and an undeclared constructor in a pattern is a compile
  error; constructors are functions; `==` compares structure; `show` prints tuples,
  constructed values and bools.
- **Phase C, the compiler typed (#86 to #102).** Hindley-Milner `rail types`; codegen reads
  representation from types (the guessing passes deleted); typed constructor fields; the parser
  builds a typed tree; `rail types tools/compile.rail` = 0 errors.
- Earlier in the window: v5.4.0 (#79), the inference engine (#58, #85), closure calls through x15
  (#84), the Linux envp fix and fleet security fixes (#60, #62, #74, #76), audits (#75, #83).
  `git log --oneline cf500f8~1..eedb1fb`
  has the full list; CHANGELOG.md (Unreleased) has the details.

## Gate (master `eedb1fb`, `set -o pipefail; ./rail_native test`)

```
  ld: OK
  PASS: ok (exit 0)
249/249 tests passed
gate rc=0
```

## Open

- **Issue #116 (APB)**, found by reading, none reproduced, riskiest first:
  1. The mark stack drops an object's children when it is full (524,288 entries): silent
     corruption on a wide heap.
  2. Grown arena pages stay resident after `arena_reset`.
  3. The `try` handler stack is shared across threads.
  4. The collector does not walk fiber stacks.
  5. An undeclared constructor or function in an expression is a link error, not a compile error.
- **Cortex-M and RISC-V** compile `_` and int-literal match arms as tag tests against -1, and
  RISC-V pops the scrutinee early. There is no emulator here to test a fix (found in phase C).
- **The site and the attest pointer lag master:** Cloudflare KV's 1,000 writes a day ran out at
  16:23 UTC on 2026-09-23. `/builds/latest` says `0307f2e`, and the site's test count says 241.
  The 06:00 `attest_daily` publishes master by itself. The site needs one deploy after 00:00 UTC.

## Next command

The site first, if it still shows 241:

```
cd ~/projects/ledatic-site && ./deploy.sh
```

Then, to resume Rail, reproduce #116 item 1 before touching the collector: a program building an
array of 600,000 short lists, run under `RAIL_GC_STRESS=13`, then `python3 tools/fuzz/gc_stress.py`.
