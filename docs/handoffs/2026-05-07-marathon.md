# 2026-05-07 — Spur Marathon Handoff

Companion summary to `training/marathon_results.md`. Read both: the results doc has tables; this doc has narrative + decisions.

## Why this happened today

- Morning: `training_pace_regression_2026-05-07` resolved (`.no_gpu` sentinel had been forcing CPU matmul fallback for 5 days). v54 pace restored — ~1.86 sec/step on the harness.
- Outside-source advice converged with internal data: Spur isn't throughput-bound; it's capability-bound. Throughput levers (fused softmax/RMSNorm/attention, mixed-precision) wouldn't move the bench number. The active frontier is structural — RLVR via the open compiler.
- The 16 scaffolds from 2026-05-01 are all on disk, all skeletons-with-implementation (100-300 lines each, all have `main =`). Marathon executes them.

## Decisions made before kickoff

- **Autonomy:** full. Surface only for compile.rail / stdlib edits, remote pushes, and stop conditions.
- **Heldout for C3:** none — C3 is **deferred**, not a marathon item.
- **Bench grid:** raw + strip × N=1 + N=20. 4 cells per ckpt. Wrapper at `tools/train/full_bench.sh`.
- **Result log:** this doc + `training/marathon_results.md`.
- **Stop conditions:** new root-cause bug (`.no_gpu`-class), 3 consecutive 0/30, or wall-clock past 60 hr.

## Wave plan

1. **Wave 0** — verify v67 (the post-fix sanity training) → bench → confirm methodology.
2. **Wave 1** — Generators + B2 + Phase 0. ~10 hr. Cheapest scaffolds, biggest data signal per hour.
3. **Wave 2** — Trainer modules requiring surgery (loss heads, sampler swaps, preference loops). ~20 hr.
4. **Wave 3** — Bench-time + heavy infra (MCTS, PBT, MoE, REPL env). Highest implementation risk; expect partials.

## Live narrative

**17:40 — Wave 0 substrate-fix discovery.**
First v67 bench ran 16:27→17:37 with raw_n1=1/30 (3%) and raw_n20=3/30 (10%) — way below v54's 9/30 baseline. Killed bench, traced root cause: `lm_infer_cpu.rail` used `matmul` (the dispatcher) at all 11 sites and depended on `tools/metal/.no_gpu` to force CPU. Removing `.no_gpu` for the training-pace fix simultaneously broke the bench substrate by routing inference through the GPU dylib (which has the unresolved sequential-inference collapse, `dylib_first_token_only`). Patched `lm_infer_cpu.rail` with a local `matmul_cpu` wrapper that bypasses `gpu_available` entirely. Substrate now self-sufficient regardless of `.no_gpu` state.

This is a `.no_gpu`-class root-cause discovery — same methodology as the morning's training-pace investigation (per `feedback_root_cause_investigation`). Memory entry written: `cpu_substrate_fix_2026-05-07.md`.

**17:40 — Wave 0 restarted with patched substrate. Wave 1 v68 (B2) training fired in parallel.**
Bench wall-clock per ckpt is ~2.3 hr (raw_n1 + raw_n20 + strip_n1 + strip_n20). 16 scaffolds × 2.3 hr = 37 hr just bench → would hit the 60-hr marathon cap. Decision: pipeline training (~1.5 hr) with prior-ckpt bench (~2.3 hr). Saves ~50% wall-clock since bench is the long pole. Two monitors armed.
