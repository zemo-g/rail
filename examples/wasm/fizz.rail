fizzbuzz n = if n > 20 then 0
  else
    let _ = if (n % 15) == 0
      then print "FizzBuzz"
      else if (n % 3) == 0
      then print "Fizz"
      else if (n % 5) == 0
      then print "Buzz"
      else print (show n)
    fizzbuzz (n + 1)

main =
  let _ = fizzbuzz 1
  0
