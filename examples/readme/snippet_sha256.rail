-- SHA-256 in pure Rail (stdlib/sha256.rail, RFC 6234 vectors in the suite).
-- Cross-check the printed digest yourself:
--   echo -n 'the loop is the proof' | shasum -a 256
import "stdlib/sha256.rail"

main =
  let _ = print (sha256_hex "the loop is the proof")
  0
