-- stdlib/env.rail — Environment functions via FFI
-- Import: import "stdlib/env.rail"

foreign getenv name -> str
foreign setenv name value flag

-- Get environment variable (returns string or "")
get_env name = getenv name

-- Set environment variable
set_env name value = setenv name value 1
