-- `%` with a float operand (2026-09-22, t224): fmod leaves raw float bits, and is_float did
-- not list `%`, so show read the bits as a pointer and crashed.
main =
  let a = 5.5
  let _ = print (show (a % 2.0))
  let _ = print (show_float (a % 2.0))
  0
