# Local cache of fleet0 pubkey is fetched on demand from
# https://ledatic.org/attest/fleet0.pub.pem on first verify.  Don't ship
# a copy in tree — keeps the source-of-truth single-rooted.
fleet0.pub.pem
