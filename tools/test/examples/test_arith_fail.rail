-- test_arith_fail.rail — intentional failure to verify the runner catches it.
main =
  let a = 2 + 2
  if a == 5 then
    let _ = print "PASS"
    0
  else
    let _ = print (cat ["FAIL: 2+2 != 5 (got ", show a, ")"])
    1
