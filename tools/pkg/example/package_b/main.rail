-- package_b main: depends on package_a via the vendor/ layout that
-- pkg_link.rail materializes from rail.toml.
import "vendor/package_a/lib.rail"

main =
  let v = double_then_add 7 5
  let _ = print (shout (cat ["double_then_add 7 5 = ", show v]))
  if v == 19 then 0 else 1
